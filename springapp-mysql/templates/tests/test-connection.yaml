apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "springapp-mysql.fullname" . }}-test-connection"
  labels:
    {{- include "springapp-mysql.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "springapp-mysql.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
