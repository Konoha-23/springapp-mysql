apiVersion: v1
kind: Service
metadata:
  name: {{ include "springapp-mysql.fullname" . }}
  labels:
    {{- include "springapp-mysql.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "springapp-mysql.selectorLabels" . | nindent 4 }}
